# Requirements per Monument Recognizer Android Build
# Ottimizzato per compatibilità Android/buildozer

# Core UI framework
kivy==2.1.0
kivymd>=1.0.0

# Image processing (Android compatible)
Pillow>=9.0.0

# Networking
requests>=2.25.0

# Mobile platform abstraction
plyer>=2.0.0

# Note: Le seguenti dipendenze sono state RIMOSSE per compatibilità Android:
# - opencv-python (problematico su Android)
# - google-cloud-vision (richiede credenziali complesse)
# - numpy (può causare problemi di build)
# - folium (dipende da web browser)
# - psutil (non supportato su Android)
# - kivy-deps.* (solo Windows)
